services:

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - ./connector_rules.yml:/etc/prometheus/connector_rules.yml
    ports:
      - "9090:9090"
    networks:
      - factory-net

networks:
  factory-net:
    external: true
