# ----------------------------------------------------------------------------------
# Prometheus scrape targets for the OpenFactory SHDR Connector
#
# Purpose:
#   This file contains the scrape targets for SHDR Coordinator and Gateway components.
#   Users can drop this file into their Prometheus setup and reference it under
#   the scrape_configs section.
#
# How to use:
#   In prometheus.yml, include it with file_sd_configs:
#
#   scrape_configs:
#     - job_name: "sshdr_connector"
#       file_sd_configs:
#         - files:
#             - "shdr_connector_scrape.yml"
#
# Notes:
#   - Each block under "targets:" defines one or more scrape targets.
#   - Labels defined here (component, role) will be attached to all metrics
#     scraped from the target.
# ----------------------------------------------------------------------------------


- targets:
    - "shdr-coordinator:4000"
  labels:
    component: "shdr_connector"
    role: "coordinator"

- targets:
    - "shdr-gateway-1:4000"
    - "shdr-gateway-2:4000"
  labels:
    component: "shdr_connector"
    role: "gateway"
